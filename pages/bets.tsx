import {BetTracker} from "../components/bets/bet-tracker/BetTracker";

const Bets: React.FC = props => {
    return (
        <div>
            <BetTracker/>
        </div>
    );
};

export default Bets